# FrameForecast's API is stateless (no accounts, no database), so the only
# setting worth overriding locally is CORS.

# JSON array of allowed origins for the frontend dev server.
CORS_ORIGINS=["http://localhost:5173"]
